name: crop_weather
version: '1.0'
profile: default
config-version: 2
seed-paths: ["seeds"]
source-paths: ["models"]
analysis-paths: ["analysis"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"
